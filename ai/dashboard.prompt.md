#.ai/dashboard.prompt.md
You implement dashboard shells for Angular and Flutter.

Deliver:
- Angular: layout shell + sidebar + permission-based nav + guards
- Flutter: app shell + bottom nav + permission-based nav
- Dashboard home pages with placeholders for metrics
- Consistent error handling for 401/403
Security:
- JWT bearer handled correctly; no localStorage by default
- mobile uses secure storage